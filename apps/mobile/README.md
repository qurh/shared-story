# shared-story mobile (reserved)

`apps/mobile` is reserved for the future client app.

Recommended stack:
- React Native + Expo
- Shared TypeScript API client with `apps/web`
- Same `/api/v1` backend contracts

This folder currently contains only the bootstrap placeholder.

